import type { ChunkMetadata } from "@/types/chat";

interface SourceCitationProps {
    source: ChunkMetadata;
}

function SourceCitation({ source }: SourceCitationProps) {
    return null;
}

export { SourceCitation };
